# FastAPI server, WebSocket, responses, templating
fastapi==0.115.6
uvicorn[standard]==0.34.0
Jinja2==3.1.5

# Request/response models
pydantic==2.10.4

# OpenCV RTSP, MJPEG encode, CSRT tracker
opencv-contrib-python-headless==4.10.0.84

# Frame/image processing
numpy==1.26.4
Pillow==12.1.1

# TensorRT-Edge-LLM runtime binaries/plugins are Jetson/CUDA/TensorRT-version specific.
# Build/install them separately, then set EDGELLM_LLM_INFERENCE_BIN if needed.
